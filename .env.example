# Core Required Variables
# MongoDB Atlas connection string
# Development: append /wmn-development, Production: append /wmn-production
DATABASE_URI=mongodb+srv://username:password@cluster.mongodb.net/wmn-development

# Used to encrypt JWT tokens (generate with: openssl rand -hex 32)
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Optional Feature Flags
# Enable/disable shop functionality
NEXT_PUBLIC_ENABLE_SHOP=false

# Use local file storage instead of Vercel Blob (true for dev, false for prod)
USE_LOCAL_STORAGE=false

# Optional: Shop/Payment Configuration (only needed if NEXT_PUBLIC_ENABLE_SHOP=true)
# STRIPE_SECRET_KEY=sk_test_your_secret_key_here
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Production Storage (only needed if USE_LOCAL_STORAGE=false)
# BLOB_READ_WRITE_TOKEN=your-vercel-blob-token

# Optional: Preview Mode
# PREVIEW_SECRET=YOUR_SECRET_HERE

# Optional: Cron Jobs
# CRON_SECRET=YOUR_CRON_SECRET_HERE

# Optional: Auto-redeployment
# VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/your-deploy-hook-url
