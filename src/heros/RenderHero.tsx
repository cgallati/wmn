import React from 'react'

// Hero sections have been removed from Pages collection
// This component is kept for backward compatibility but always returns null
export const RenderHero: React.FC<any> = () => {
  return null
}
